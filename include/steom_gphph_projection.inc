// AUTO-GENERATED by script/steom_gphph_cppgen.py — DO NOT EDIT.
// Projection g_phph/g_phhp off-diagonal routes (STEOM overshoot fix).
// Fills proj_route_ph[i,a,j,b] (= Mc-Ms) and proj_route_mc[i,a,j,b] (= Mc).
// Scope requirements: NO,NV,NMo,NMv, active_occ_idx_, active_vir_idx_,
//   lambdas fov,wooov,wvovv,eriov,siP,seA; vectors proj_route_ph, proj_route_mc.
// Derivation/validation: memory project_dlpno_steom_orca_reconsider step1 1-37;
//   gates: H2O FC1 + hexatriene pi-CAS eigenvalues = det oracle (1e-8 eV).

    // ======== proj_route_ph: 96 terms ========
    { // term ph000: +0.500000 * Fov[j,c] * ry[b,i,c,a]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                acc += fov(q_j,q_c)*seA(r_b,q_i,q_c,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph001: -1.500000 * Fov[j,c] * ry[b,i,a,c]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                acc += fov(q_j,q_c)*seA(r_b,q_i,q_a,q_c);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.5000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph002: -1.500000 * Wvovv[a,j,c,d] * ry[b,i,c,d]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += wvovv(q_a,q_j,q_c,q_d)*seA(r_b,q_i,q_c,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.5000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph003: +0.500000 * Wvovv[a,j,c,d] * ry[b,i,d,c]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += wvovv(q_a,q_j,q_c,q_d)*seA(r_b,q_i,q_d,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph004: -0.500000 * Wooov[k,j,i,c] * ry[b,k,c,a]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += wooov(q_k,q_j,q_i,q_c)*seA(r_b,q_k,q_c,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph005: +1.500000 * Wooov[k,j,i,c] * ry[b,k,a,c]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += wooov(q_k,q_j,q_i,q_c)*seA(r_b,q_k,q_a,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph006: -1.125000 * eri_ovov[j,c,k,d] * ry[e,i,a,d] * ry[b,k,e,c]
    std::vector<real_t> tph006((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_e,q_c);
                }
              }
              tph006[(((r_b)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph006[(((r_b)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_a,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.1250000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph007: +1.125000 * eri_ovov[j,c,k,d] * ry[e,i,a,c] * ry[b,k,e,d]
    std::vector<real_t> tph007((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_e,q_d);
                }
              }
              tph007[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph007[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_a,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.1250000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph008: -0.375000 * eri_ovov[j,c,k,d] * ry[e,i,a,c] * ry[b,k,d,e]
    std::vector<real_t> tph008((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_d,q_e);
                }
              }
              tph008[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph008[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_a,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph009: -0.500000 * eri_ovov[j,c,k,d] * ry[e,i,d,c] * ry[b,k,e,a]
    std::vector<real_t> tph009((size_t)NMv*NO*NO*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_d,q_c);
                }
              }
              tph009[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph009[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k]*seA(r_b,q_k,q_e,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph010: +0.500000 * eri_ovov[j,c,k,d] * ry[e,i,c,d] * ry[b,k,e,a]
    std::vector<real_t> tph010((size_t)NMv*NO*NO*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_c,q_d);
                }
              }
              tph010[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph010[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k]*seA(r_b,q_k,q_e,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph011: -0.375000 * eri_ovov[j,c,k,d] * ry[e,k,d,a] * ry[b,i,e,c]
    std::vector<real_t> tph011((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_a);
                }
              }
              tph011[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph011[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_e,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph012: +1.125000 * eri_ovov[j,c,k,d] * ry[e,k,a,d] * ry[b,i,e,c]
    std::vector<real_t> tph012((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_d);
                }
              }
              tph012[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph012[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_e,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.1250000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph013: +0.250000 * eri_ovov[j,c,k,d] * ry[e,k,c,a] * ry[b,i,e,d]
    std::vector<real_t> tph013((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_a);
                }
              }
              tph013[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph013[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_e,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph014: -0.125000 * eri_ovov[j,c,k,d] * ry[e,i,d,a] * ry[b,k,c,e]
    std::vector<real_t> tph014((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_c,q_e);
                }
              }
              tph014[(((r_b)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph014[(((r_b)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_d,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph015: +0.125000 * eri_ovov[j,c,k,d] * ry[e,i,c,a] * ry[b,k,d,e]
    std::vector<real_t> tph015((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_d,q_e);
                }
              }
              tph015[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph015[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_c,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph016: -0.375000 * eri_ovov[j,c,k,d] * ry[e,i,c,a] * ry[b,k,e,d]
    std::vector<real_t> tph016((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_e,q_d);
                }
              }
              tph016[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph016[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_c,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph017: -0.375000 * eri_ovov[j,c,k,d] * ry[e,k,a,d] * ry[b,i,c,e]
    std::vector<real_t> tph017((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_d);
                }
              }
              tph017[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph017[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_c,q_e);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph018: +0.125000 * eri_ovov[j,c,k,d] * ry[e,k,d,a] * ry[b,i,c,e]
    std::vector<real_t> tph018((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_a);
                }
              }
              tph018[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph018[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_c,q_e);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph019: +0.250000 * eri_ovov[j,c,k,d] * ry[e,k,a,c] * ry[b,i,d,e]
    std::vector<real_t> tph019((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_c);
                }
              }
              tph019[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph019[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_d,q_e);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph020: -0.375000 * eri_ovov[j,c,k,d] * ry[e,k,d,c] * ry[b,i,a,e]
    std::vector<real_t> tph020((size_t)NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_j=0;q_j<NO;++q_j){
          real_t acc=0.0;
          for (int q_c=0;q_c<NV;++q_c){
            for (int q_k=0;q_k<NO;++q_k){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_c);
              }
            }
          }
          tph020[(r_e)*NO+q_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                acc += tph020[(r_e)*NO+q_j]*seA(r_b,q_i,q_a,q_e);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph021: +1.125000 * eri_ovov[j,c,k,d] * ry[e,k,c,d] * ry[b,i,a,e]
    std::vector<real_t> tph021((size_t)NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_j=0;q_j<NO;++q_j){
          real_t acc=0.0;
          for (int q_c=0;q_c<NV;++q_c){
            for (int q_k=0;q_k<NO;++q_k){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_d);
              }
            }
          }
          tph021[(r_e)*NO+q_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                acc += tph021[(r_e)*NO+q_j]*seA(r_b,q_i,q_a,q_e);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.1250000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph022: -0.750000 * eri_ovov[j,c,k,d] * ry[e,k,a,c] * ry[b,i,e,d]
    std::vector<real_t> tph022((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_c);
                }
              }
              tph022[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph022[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_e,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph023: +0.125000 * eri_ovov[j,c,k,d] * ry[e,k,d,c] * ry[b,i,e,a]
    std::vector<real_t> tph023((size_t)NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_j=0;q_j<NO;++q_j){
          real_t acc=0.0;
          for (int q_c=0;q_c<NV;++q_c){
            for (int q_k=0;q_k<NO;++q_k){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_c);
              }
            }
          }
          tph023[(r_e)*NO+q_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                acc += tph023[(r_e)*NO+q_j]*seA(r_b,q_i,q_e,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph024: -0.375000 * eri_ovov[j,c,k,d] * ry[e,k,c,d] * ry[b,i,e,a]
    std::vector<real_t> tph024((size_t)NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_j=0;q_j<NO;++q_j){
          real_t acc=0.0;
          for (int q_c=0;q_c<NV;++q_c){
            for (int q_k=0;q_k<NO;++q_k){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_d);
              }
            }
          }
          tph024[(r_e)*NO+q_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                acc += tph024[(r_e)*NO+q_j]*seA(r_b,q_i,q_e,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph025: +0.500000 * eri_ovov[j,c,k,d] * ry[e,i,d,c] * ry[b,k,a,e]
    std::vector<real_t> tph025((size_t)NMv*NO*NO*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_d,q_c);
                }
              }
              tph025[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph025[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k]*seA(r_b,q_k,q_a,q_e);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph026: -0.500000 * eri_ovov[j,c,k,d] * ry[e,i,c,d] * ry[b,k,a,e]
    std::vector<real_t> tph026((size_t)NMv*NO*NO*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_c,q_d);
                }
              }
              tph026[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph026[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k]*seA(r_b,q_k,q_a,q_e);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph027: -0.750000 * eri_ovov[j,c,k,d] * ry[e,k,c,a] * ry[b,i,d,e]
    std::vector<real_t> tph027((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_a);
                }
              }
              tph027[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph027[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_d,q_e);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph028: -0.750000 * eri_ovov[k,c,l,b] * rx[j,k,i,d] * ry[d,l,c,a]
    std::vector<real_t> tph028((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*seA(r_d,q_l,q_c,q_a);
                }
              }
              tph028[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tph028[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k]*siP(r_j,q_k,q_i,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph029: +0.250000 * eri_ovov[k,c,l,b] * rx[j,k,i,d] * ry[d,l,a,c]
    std::vector<real_t> tph029((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*seA(r_d,q_l,q_a,q_c);
                }
              }
              tph029[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tph029[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k]*siP(r_j,q_k,q_i,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph030: +0.125000 * eri_ovov[k,b,l,c] * rx[j,l,k,d] * ry[d,i,c,a]
    std::vector<real_t> tph030((size_t)NV*NV*NMv*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_l,q_k,q_d);
                }
              }
              tph030[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tph030[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j]*seA(r_d,q_i,q_c,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph031: +0.125000 * eri_ovov[k,c,l,b] * rx[j,k,l,d] * ry[d,i,c,a]
    std::vector<real_t> tph031((size_t)NV*NV*NMv*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_k,q_l,q_d);
                }
              }
              tph031[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tph031[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j]*seA(r_d,q_i,q_c,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph032: -1.500000 * eri_ovov[k,c,l,d] * rx[j,k,i,d] * ry[b,l,a,c]
    std::vector<real_t> tph032((size_t)NV*NO*NMo*NO, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_l=0;q_l<NO;++q_l){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_d);
                }
              }
              tph032[(((q_c)*NO+q_i)*NMo+r_j)*NO+q_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += tph032[(((q_c)*NO+q_i)*NMo+r_j)*NO+q_l]*seA(r_b,q_l,q_a,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.5000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph033: +0.500000 * eri_ovov[k,c,l,d] * rx[j,k,i,d] * ry[b,l,c,a]
    std::vector<real_t> tph033((size_t)NV*NO*NMo*NO, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_l=0;q_l<NO;++q_l){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_d);
                }
              }
              tph033[(((q_c)*NO+q_i)*NMo+r_j)*NO+q_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += tph033[(((q_c)*NO+q_i)*NMo+r_j)*NO+q_l]*seA(r_b,q_l,q_c,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph034: +1.500000 * eri_ovov[k,c,l,d] * rx[j,l,k,c] * ry[b,i,a,d]
    std::vector<real_t> tph034((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_c=0;q_c<NV;++q_c){
              for (int q_l=0;q_l<NO;++q_l){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_c);
              }
            }
          }
          tph034[(q_d)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                acc += tph034[(q_d)*NMo+r_j]*seA(r_b,q_i,q_a,q_d);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph035: +1.500000 * eri_ovov[k,c,l,d] * rx[j,k,l,d] * ry[b,i,a,c]
    std::vector<real_t> tph035((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_d);
              }
            }
          }
          tph035[(q_c)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                acc += tph035[(q_c)*NMo+r_j]*seA(r_b,q_i,q_a,q_c);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph036: -0.750000 * eri_ovov[k,c,l,d] * rx[j,k,l,c] * ry[b,i,a,d]
    std::vector<real_t> tph036((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_c=0;q_c<NV;++q_c){
              for (int q_l=0;q_l<NO;++q_l){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_c);
              }
            }
          }
          tph036[(q_d)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                acc += tph036[(q_d)*NMo+r_j]*seA(r_b,q_i,q_a,q_d);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph037: -0.750000 * eri_ovov[k,c,l,d] * rx[j,l,k,d] * ry[b,i,a,c]
    std::vector<real_t> tph037((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_d);
              }
            }
          }
          tph037[(q_c)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                acc += tph037[(q_c)*NMo+r_j]*seA(r_b,q_i,q_a,q_c);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph038: -0.375000 * eri_ovov[k,b,l,c] * rx[j,l,k,d] * ry[d,i,a,c]
    std::vector<real_t> tph038((size_t)NV*NV*NMv*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_l,q_k,q_d);
                }
              }
              tph038[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tph038[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j]*seA(r_d,q_i,q_a,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph039: -0.375000 * eri_ovov[k,c,l,b] * rx[j,k,l,d] * ry[d,i,a,c]
    std::vector<real_t> tph039((size_t)NV*NV*NMv*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_k,q_l,q_d);
                }
              }
              tph039[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tph039[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j]*seA(r_d,q_i,q_a,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph040: -0.750000 * eri_ovov[k,c,l,d] * rx[j,l,k,a] * ry[b,i,c,d]
    std::vector<real_t> tph040((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int q_d=0;q_d<NV;++q_d){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_a);
                }
              }
              tph040[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += tph040[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j]*seA(r_b,q_i,q_c,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph041: +0.250000 * eri_ovov[k,c,l,d] * rx[j,l,k,a] * ry[b,i,d,c]
    std::vector<real_t> tph041((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int q_d=0;q_d<NV;++q_d){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_a);
                }
              }
              tph041[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += tph041[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j]*seA(r_b,q_i,q_d,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph042: -0.750000 * eri_ovov[k,c,l,d] * rx[j,k,l,a] * ry[b,i,d,c]
    std::vector<real_t> tph042((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int q_d=0;q_d<NV;++q_d){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_a);
                }
              }
              tph042[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += tph042[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j]*seA(r_b,q_i,q_d,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph043: +0.250000 * eri_ovov[k,c,l,d] * rx[j,k,l,a] * ry[b,i,c,d]
    std::vector<real_t> tph043((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int q_d=0;q_d<NV;++q_d){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_a);
                }
              }
              tph043[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += tph043[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j]*seA(r_b,q_i,q_c,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph044: -0.500000 * eri_ovov[k,c,l,d] * rx[j,l,k,c] * ry[b,i,d,a]
    std::vector<real_t> tph044((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_c=0;q_c<NV;++q_c){
              for (int q_l=0;q_l<NO;++q_l){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_c);
              }
            }
          }
          tph044[(q_d)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                acc += tph044[(q_d)*NMo+r_j]*seA(r_b,q_i,q_d,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph045: -0.500000 * eri_ovov[k,c,l,d] * rx[j,k,l,d] * ry[b,i,c,a]
    std::vector<real_t> tph045((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_d);
              }
            }
          }
          tph045[(q_c)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                acc += tph045[(q_c)*NMo+r_j]*seA(r_b,q_i,q_c,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph046: +0.250000 * eri_ovov[k,c,l,d] * rx[j,k,l,c] * ry[b,i,d,a]
    std::vector<real_t> tph046((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_c=0;q_c<NV;++q_c){
              for (int q_l=0;q_l<NO;++q_l){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_c);
              }
            }
          }
          tph046[(q_d)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                acc += tph046[(q_d)*NMo+r_j]*seA(r_b,q_i,q_d,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph047: +0.250000 * eri_ovov[k,c,l,d] * rx[j,l,k,d] * ry[b,i,c,a]
    std::vector<real_t> tph047((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_d);
              }
            }
          }
          tph047[(q_c)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                acc += tph047[(q_c)*NMo+r_j]*seA(r_b,q_i,q_c,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph048: -0.750000 * eri_ovov[j,c,k,d] * rx[l,k,i,d] * ry[b,l,a,c]
    std::vector<real_t> tph048((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_d);
                }
              }
              tph048[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tph048[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_a,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph049: +1.500000 * eri_ovov[j,c,k,d] * rx[l,i,k,d] * ry[b,l,a,c]
    std::vector<real_t> tph049((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_d);
                }
              }
              tph049[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tph049[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_a,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph050: -0.750000 * eri_ovov[j,c,k,d] * rx[l,i,k,c] * ry[b,l,a,d]
    std::vector<real_t> tph050((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_c);
                }
              }
              tph050[(((q_d)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tph050[(((q_d)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_a,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph051: -0.750000 * eri_ovov[j,c,k,d] * rx[l,i,k,a] * ry[b,l,d,c]
    std::vector<real_t> tph051((size_t)NMv*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_j=0;q_j<NO;++q_j){
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_l,q_d,q_c);
                }
              }
              tph051[(((r_b)*NO+q_j)*NO+q_k)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tph051[(((r_b)*NO+q_j)*NO+q_k)*NMo+r_l]*siP(r_l,q_i,q_k,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph052: +0.250000 * eri_ovov[j,c,k,d] * rx[l,i,k,a] * ry[b,l,c,d]
    std::vector<real_t> tph052((size_t)NMv*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_j=0;q_j<NO;++q_j){
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_l,q_c,q_d);
                }
              }
              tph052[(((r_b)*NO+q_j)*NO+q_k)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tph052[(((r_b)*NO+q_j)*NO+q_k)*NMo+r_l]*siP(r_l,q_i,q_k,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph053: -0.500000 * eri_ovov[j,c,k,d] * rx[l,i,k,d] * ry[b,l,c,a]
    std::vector<real_t> tph053((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_d);
                }
              }
              tph053[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tph053[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_c,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph054: +0.250000 * eri_ovov[j,c,k,d] * rx[l,k,i,d] * ry[b,l,c,a]
    std::vector<real_t> tph054((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_d);
                }
              }
              tph054[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tph054[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_c,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph055: +0.250000 * eri_ovov[j,c,k,d] * rx[l,i,k,c] * ry[b,l,d,a]
    std::vector<real_t> tph055((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_c);
                }
              }
              tph055[(((q_d)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tph055[(((q_d)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_d,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph056: -1.000000 * Fov[k,b] * rx[j,i,k,a]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                acc += fov(q_k,q_b)*siP(r_j,q_i,q_k,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph057: +1.000000 * Wooov[k,l,i,b] * rx[j,k,l,a]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += wooov(q_k,q_l,q_i,q_b)*siP(r_j,q_k,q_l,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph058: +2.000000 * Wvovv[a,k,b,c] * rx[j,i,k,c]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += wvovv(q_a,q_k,q_b,q_c)*siP(r_j,q_i,q_k,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph059: -1.000000 * Wvovv[a,k,b,c] * rx[j,k,i,c]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += wvovv(q_a,q_k,q_b,q_c)*siP(r_j,q_k,q_i,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph060: -1.000000 * Wvovv[a,k,c,b] * rx[j,i,k,c]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += wvovv(q_a,q_k,q_c,q_b)*siP(r_j,q_i,q_k,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph061: +0.500000 * Wooov[j,k,i,c] * ry[b,k,c,a]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += wooov(q_j,q_k,q_i,q_c)*seA(r_b,q_k,q_c,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph062: -1.500000 * Wooov[j,k,i,c] * ry[b,k,a,c]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += wooov(q_j,q_k,q_i,q_c)*seA(r_b,q_k,q_a,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.5000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph063: -0.500000 * eri_ovov[k,b,l,c] * rx[m,i,k,a] * rx[j,l,m,c]
    std::vector<real_t> tph063((size_t)NV*NMo*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_l,q_m,q_c);
                }
              }
              tph063[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tph063[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m]*siP(r_m,q_i,q_k,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph064: +1.000000 * eri_ovov[k,b,l,c] * rx[m,i,k,a] * rx[j,m,l,c]
    std::vector<real_t> tph064((size_t)NV*NMo*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_m,q_l,q_c);
                }
              }
              tph064[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tph064[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m]*siP(r_m,q_i,q_k,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph065: -0.500000 * eri_ovov[k,c,l,b] * rx[m,i,k,a] * rx[j,m,l,c]
    std::vector<real_t> tph065((size_t)NV*NMo*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_m,q_l,q_c);
                }
              }
              tph065[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tph065[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m]*siP(r_m,q_i,q_k,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph066: -0.500000 * eri_ovov[k,b,l,c] * rx[m,i,k,c] * rx[j,m,l,a]
    std::vector<real_t> tph066((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_l=0;q_l<NO;++q_l){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_i,q_k,q_c);
                }
              }
              tph066[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tph066[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m]*siP(r_j,q_m,q_l,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph067: -0.500000 * eri_ovov[k,b,l,c] * rx[m,k,i,c] * rx[j,l,m,a]
    std::vector<real_t> tph067((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_l=0;q_l<NO;++q_l){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_i,q_c);
                }
              }
              tph067[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tph067[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m]*siP(r_j,q_l,q_m,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph068: +1.000000 * eri_ovov[k,c,l,b] * rx[m,i,k,c] * rx[j,m,l,a]
    std::vector<real_t> tph068((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_l=0;q_l<NO;++q_l){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_i,q_k,q_c);
                }
              }
              tph068[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tph068[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m]*siP(r_j,q_m,q_l,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph069: -0.500000 * eri_ovov[k,c,l,b] * rx[m,k,i,c] * rx[j,m,l,a]
    std::vector<real_t> tph069((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_l=0;q_l<NO;++q_l){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_i,q_c);
                }
              }
              tph069[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tph069[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m]*siP(r_j,q_m,q_l,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph070: +0.500000 * eri_ovov[k,b,l,c] * rx[m,l,k,a] * rx[j,i,m,c]
    std::vector<real_t> tph070((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int q_c=0;q_c<NV;++q_c){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_l,q_k,q_a);
                }
              }
              tph070[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tph070[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m]*siP(r_j,q_i,q_m,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph071: -0.250000 * eri_ovov[k,b,l,c] * rx[m,l,k,a] * rx[j,m,i,c]
    std::vector<real_t> tph071((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int q_c=0;q_c<NV;++q_c){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_l,q_k,q_a);
                }
              }
              tph071[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tph071[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m]*siP(r_j,q_m,q_i,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph072: -0.250000 * eri_ovov[k,b,l,c] * rx[m,k,l,a] * rx[j,i,m,c]
    std::vector<real_t> tph072((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int q_c=0;q_c<NV;++q_c){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_l,q_a);
                }
              }
              tph072[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tph072[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m]*siP(r_j,q_i,q_m,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph073: -0.250000 * eri_ovov[k,c,l,b] * rx[m,l,k,a] * rx[j,i,m,c]
    std::vector<real_t> tph073((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int q_c=0;q_c<NV;++q_c){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_l,q_k,q_a);
                }
              }
              tph073[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tph073[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m]*siP(r_j,q_i,q_m,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph074: +0.500000 * eri_ovov[k,c,l,b] * rx[m,k,l,a] * rx[j,i,m,c]
    std::vector<real_t> tph074((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int q_c=0;q_c<NV;++q_c){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_l,q_a);
                }
              }
              tph074[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tph074[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m]*siP(r_j,q_i,q_m,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph075: -0.250000 * eri_ovov[k,c,l,b] * rx[m,k,l,a] * rx[j,m,i,c]
    std::vector<real_t> tph075((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int q_c=0;q_c<NV;++q_c){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_l,q_a);
                }
              }
              tph075[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tph075[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m]*siP(r_j,q_m,q_i,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph076: -0.250000 * eri_ovov[k,b,l,c] * rx[m,l,k,c] * rx[j,i,m,a]
    std::vector<real_t> tph076((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_c=0;q_c<NV;++q_c){
                acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_l,q_k,q_c);
              }
            }
          }
          tph076[(q_b)*NMo+r_m] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                acc += tph076[(q_b)*NMo+r_m]*siP(r_j,q_i,q_m,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph077: +0.500000 * eri_ovov[k,b,l,c] * rx[m,k,l,c] * rx[j,i,m,a]
    std::vector<real_t> tph077((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_c=0;q_c<NV;++q_c){
                acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_l,q_c);
              }
            }
          }
          tph077[(q_b)*NMo+r_m] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                acc += tph077[(q_b)*NMo+r_m]*siP(r_j,q_i,q_m,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph078: +0.500000 * eri_ovov[k,c,l,b] * rx[m,l,k,c] * rx[j,i,m,a]
    std::vector<real_t> tph078((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_c=0;q_c<NV;++q_c){
              for (int q_l=0;q_l<NO;++q_l){
                acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_l,q_k,q_c);
              }
            }
          }
          tph078[(q_b)*NMo+r_m] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                acc += tph078[(q_b)*NMo+r_m]*siP(r_j,q_i,q_m,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph079: -0.250000 * eri_ovov[k,c,l,b] * rx[m,k,l,c] * rx[j,i,m,a]
    std::vector<real_t> tph079((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_c=0;q_c<NV;++q_c){
              for (int q_l=0;q_l<NO;++q_l){
                acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_l,q_c);
              }
            }
          }
          tph079[(q_b)*NMo+r_m] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                acc += tph079[(q_b)*NMo+r_m]*siP(r_j,q_i,q_m,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph080: +0.375000 * eri_ovov[j,c,k,d] * ry[e,i,a,d] * ry[b,k,c,e]
    std::vector<real_t> tph080((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_c,q_e);
                }
              }
              tph080[(((r_b)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph080[(((r_b)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_a,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph081: +0.375000 * eri_ovov[j,c,k,d] * ry[e,i,d,a] * ry[b,k,e,c]
    std::vector<real_t> tph081((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_e,q_c);
                }
              }
              tph081[(((r_b)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tph081[(((r_b)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_d,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph082: -0.250000 * eri_ovov[k,b,l,c] * rx[j,i,k,d] * ry[d,l,c,a]
    std::vector<real_t> tph082((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_b,q_l,q_c)*seA(r_d,q_l,q_c,q_a);
                }
              }
              tph082[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tph082[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k]*siP(r_j,q_i,q_k,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph083: +0.750000 * eri_ovov[k,b,l,c] * rx[j,i,k,d] * ry[d,l,a,c]
    std::vector<real_t> tph083((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_b,q_l,q_c)*seA(r_d,q_l,q_a,q_c);
                }
              }
              tph083[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tph083[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k]*siP(r_j,q_i,q_k,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph084: +0.750000 * eri_ovov[k,c,l,b] * rx[j,i,k,d] * ry[d,l,c,a]
    std::vector<real_t> tph084((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*seA(r_d,q_l,q_c,q_a);
                }
              }
              tph084[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tph084[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k]*siP(r_j,q_i,q_k,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph085: -0.250000 * eri_ovov[k,c,l,b] * rx[j,i,k,d] * ry[d,l,a,c]
    std::vector<real_t> tph085((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*seA(r_d,q_l,q_a,q_c);
                }
              }
              tph085[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tph085[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k]*siP(r_j,q_i,q_k,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph086: +1.500000 * eri_ovov[k,c,l,d] * rx[j,k,i,c] * ry[b,l,a,d]
    std::vector<real_t> tph086((size_t)NV*NO*NMo*NO, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int q_i=0;q_i<NO;++q_i){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_l=0;q_l<NO;++q_l){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_c);
                }
              }
              tph086[(((q_d)*NO+q_i)*NMo+r_j)*NO+q_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += tph086[(((q_d)*NO+q_i)*NMo+r_j)*NO+q_l]*seA(r_b,q_l,q_a,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph087: -0.500000 * eri_ovov[k,c,l,d] * rx[j,k,i,c] * ry[b,l,d,a]
    std::vector<real_t> tph087((size_t)NV*NO*NMo*NO, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int q_i=0;q_i<NO;++q_i){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_l=0;q_l<NO;++q_l){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_c);
                }
              }
              tph087[(((q_d)*NO+q_i)*NMo+r_j)*NO+q_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += tph087[(((q_d)*NO+q_i)*NMo+r_j)*NO+q_l]*seA(r_b,q_l,q_d,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph088: -3.000000 * eri_ovov[k,c,l,d] * rx[j,i,k,c] * ry[b,l,a,d]
    std::vector<real_t> tph088((size_t)NV*NO*NMo*NO, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int q_i=0;q_i<NO;++q_i){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_l=0;q_l<NO;++q_l){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_i,q_k,q_c);
                }
              }
              tph088[(((q_d)*NO+q_i)*NMo+r_j)*NO+q_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += tph088[(((q_d)*NO+q_i)*NMo+r_j)*NO+q_l]*seA(r_b,q_l,q_a,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph089: +1.000000 * eri_ovov[k,c,l,d] * rx[j,i,k,c] * ry[b,l,d,a]
    std::vector<real_t> tph089((size_t)NV*NO*NMo*NO, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int q_i=0;q_i<NO;++q_i){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_l=0;q_l<NO;++q_l){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_i,q_k,q_c);
                }
              }
              tph089[(((q_d)*NO+q_i)*NMo+r_j)*NO+q_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += tph089[(((q_d)*NO+q_i)*NMo+r_j)*NO+q_l]*seA(r_b,q_l,q_d,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph090: +1.500000 * eri_ovov[k,c,l,d] * rx[j,i,k,d] * ry[b,l,a,c]
    std::vector<real_t> tph090((size_t)NV*NO*NMo*NO, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_l=0;q_l<NO;++q_l){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_i,q_k,q_d);
                }
              }
              tph090[(((q_c)*NO+q_i)*NMo+r_j)*NO+q_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += tph090[(((q_c)*NO+q_i)*NMo+r_j)*NO+q_l]*seA(r_b,q_l,q_a,q_c);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph091: -0.500000 * eri_ovov[k,c,l,d] * rx[j,i,k,d] * ry[b,l,c,a]
    std::vector<real_t> tph091((size_t)NV*NO*NMo*NO, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_l=0;q_l<NO;++q_l){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_i,q_k,q_d);
                }
              }
              tph091[(((q_c)*NO+q_i)*NMo+r_j)*NO+q_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += tph091[(((q_c)*NO+q_i)*NMo+r_j)*NO+q_l]*seA(r_b,q_l,q_c,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph092: +1.500000 * eri_ovov[k,c,l,d] * rx[j,i,k,a] * ry[b,l,c,d]
    std::vector<real_t> tph092((size_t)NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_k=0;q_k<NO;++q_k){
          real_t acc=0.0;
          for (int q_c=0;q_c<NV;++q_c){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_k,q_c,q_l,q_d)*seA(r_b,q_l,q_c,q_d);
              }
            }
          }
          tph092[(r_b)*NO+q_k] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                acc += tph092[(r_b)*NO+q_k]*siP(r_j,q_i,q_k,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term ph093: -0.500000 * eri_ovov[k,c,l,d] * rx[j,i,k,a] * ry[b,l,d,c]
    std::vector<real_t> tph093((size_t)NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_k=0;q_k<NO;++q_k){
          real_t acc=0.0;
          for (int q_c=0;q_c<NV;++q_c){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_k,q_c,q_l,q_d)*seA(r_b,q_l,q_d,q_c);
              }
            }
          }
          tph093[(r_b)*NO+q_k] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                acc += tph093[(r_b)*NO+q_k]*siP(r_j,q_i,q_k,q_a);
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph094: +0.750000 * eri_ovov[j,c,k,d] * rx[l,k,i,c] * ry[b,l,a,d]
    std::vector<real_t> tph094((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_c);
                }
              }
              tph094[(((q_d)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tph094[(((q_d)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_a,q_d);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term ph095: -0.250000 * eri_ovov[j,c,k,d] * rx[l,k,i,c] * ry[b,l,d,a]
    std::vector<real_t> tph095((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_c);
                }
              }
              tph095[(((q_d)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tph095[(((q_d)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_d,q_a);
                }
              }
              proj_route_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    // ======== proj_route_mc: 84 terms ========
    { // term mc000: -1.000000 * Fov[k,b] * rx[j,k,i,a]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                acc += fov(q_k,q_b)*siP(r_j,q_k,q_i,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term mc001: +1.000000 * Wooov[k,l,i,b] * rx[j,l,k,a]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += wooov(q_k,q_l,q_i,q_b)*siP(r_j,q_l,q_k,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term mc002: -1.000000 * Wvovv[a,k,c,b] * rx[j,k,i,c]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += wvovv(q_a,q_k,q_c,q_b)*siP(r_j,q_k,q_i,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term mc003: -0.500000 * Fov[j,c] * ry[b,i,c,a]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                acc += fov(q_j,q_c)*seA(r_b,q_i,q_c,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc004: -0.500000 * Fov[j,c] * ry[b,i,a,c]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                acc += fov(q_j,q_c)*seA(r_b,q_i,q_a,q_c);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc005: -0.500000 * Wvovv[a,j,c,d] * ry[b,i,c,d]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += wvovv(q_a,q_j,q_c,q_d)*seA(r_b,q_i,q_c,q_d);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc006: -0.500000 * Wvovv[a,j,c,d] * ry[b,i,d,c]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += wvovv(q_a,q_j,q_c,q_d)*seA(r_b,q_i,q_d,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc007: +0.500000 * Wooov[k,j,i,c] * ry[b,k,c,a]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += wooov(q_k,q_j,q_i,q_c)*seA(r_b,q_k,q_c,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc008: +0.500000 * Wooov[k,j,i,c] * ry[b,k,a,c]
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += wooov(q_k,q_j,q_i,q_c)*seA(r_b,q_k,q_a,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc009: +1.000000 * eri_ovov[k,c,l,b] * rx[m,k,i,a] * rx[j,l,m,c]
    std::vector<real_t> tmc009((size_t)NV*NMo*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_l,q_m,q_c);
                }
              }
              tmc009[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tmc009[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m]*siP(r_m,q_k,q_i,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term mc010: -0.500000 * eri_ovov[k,c,l,b] * rx[m,i,k,a] * rx[j,l,m,c]
    std::vector<real_t> tmc010((size_t)NV*NMo*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_l,q_m,q_c);
                }
              }
              tmc010[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tmc010[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m]*siP(r_m,q_i,q_k,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc011: +1.000000 * eri_ovov[k,b,l,c] * rx[m,k,i,a] * rx[j,m,l,c]
    std::vector<real_t> tmc011((size_t)NV*NMo*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_m,q_l,q_c);
                }
              }
              tmc011[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tmc011[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m]*siP(r_m,q_k,q_i,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term mc012: -0.500000 * eri_ovov[k,b,l,c] * rx[m,k,i,a] * rx[j,l,m,c]
    std::vector<real_t> tmc012((size_t)NV*NMo*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_l,q_m,q_c);
                }
              }
              tmc012[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tmc012[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m]*siP(r_m,q_k,q_i,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc013: -0.500000 * eri_ovov[k,c,l,b] * rx[m,k,i,a] * rx[j,m,l,c]
    std::vector<real_t> tmc013((size_t)NV*NMo*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_m,q_l,q_c);
                }
              }
              tmc013[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tmc013[(((q_b)*NMo+r_j)*NO+q_k)*NMo+r_m]*siP(r_m,q_k,q_i,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc014: +1.000000 * eri_ovov[k,c,l,b] * rx[m,i,k,c] * rx[j,l,m,a]
    std::vector<real_t> tmc014((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_l=0;q_l<NO;++q_l){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_i,q_k,q_c);
                }
              }
              tmc014[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tmc014[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m]*siP(r_j,q_l,q_m,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term mc015: -0.500000 * eri_ovov[k,c,l,b] * rx[m,k,i,c] * rx[j,l,m,a]
    std::vector<real_t> tmc015((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_l=0;q_l<NO;++q_l){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_i,q_c);
                }
              }
              tmc015[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tmc015[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m]*siP(r_j,q_l,q_m,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc016: -0.500000 * eri_ovov[k,b,l,c] * rx[m,k,i,c] * rx[j,m,l,a]
    std::vector<real_t> tmc016((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_l=0;q_l<NO;++q_l){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_i,q_c);
                }
              }
              tmc016[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tmc016[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m]*siP(r_j,q_m,q_l,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc017: -0.500000 * eri_ovov[k,b,l,c] * rx[m,i,k,c] * rx[j,l,m,a]
    std::vector<real_t> tmc017((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_l=0;q_l<NO;++q_l){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_i,q_k,q_c);
                }
              }
              tmc017[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tmc017[(((q_b)*NO+q_i)*NO+q_l)*NMo+r_m]*siP(r_j,q_l,q_m,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc018: -0.250000 * eri_ovov[k,c,l,b] * rx[m,l,k,a] * rx[j,m,i,c]
    std::vector<real_t> tmc018((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int q_c=0;q_c<NV;++q_c){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_l,q_k,q_a);
                }
              }
              tmc018[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tmc018[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m]*siP(r_j,q_m,q_i,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc019: -0.250000 * eri_ovov[k,b,l,c] * rx[m,k,l,a] * rx[j,m,i,c]
    std::vector<real_t> tmc019((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int q_c=0;q_c<NV;++q_c){
            for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_l,q_a);
                }
              }
              tmc019[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                  acc += tmc019[(((q_a)*NV+q_b)*NV+q_c)*NMo+r_m]*siP(r_j,q_m,q_i,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc020: +0.500000 * eri_ovov[k,c,l,b] * rx[m,l,k,c] * rx[j,m,i,a]
    std::vector<real_t> tmc020((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_c=0;q_c<NV;++q_c){
              for (int q_l=0;q_l<NO;++q_l){
                acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_l,q_k,q_c);
              }
            }
          }
          tmc020[(q_b)*NMo+r_m] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                acc += tmc020[(q_b)*NMo+r_m]*siP(r_j,q_m,q_i,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc021: +0.500000 * eri_ovov[k,b,l,c] * rx[m,k,l,c] * rx[j,m,i,a]
    std::vector<real_t> tmc021((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_c=0;q_c<NV;++q_c){
                acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_l,q_c);
              }
            }
          }
          tmc021[(q_b)*NMo+r_m] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                acc += tmc021[(q_b)*NMo+r_m]*siP(r_j,q_m,q_i,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc022: -0.250000 * eri_ovov[k,b,l,c] * rx[m,l,k,c] * rx[j,m,i,a]
    std::vector<real_t> tmc022((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_c=0;q_c<NV;++q_c){
                acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_l,q_k,q_c);
              }
            }
          }
          tmc022[(q_b)*NMo+r_m] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                acc += tmc022[(q_b)*NMo+r_m]*siP(r_j,q_m,q_i,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc023: -0.250000 * eri_ovov[k,c,l,b] * rx[m,k,l,c] * rx[j,m,i,a]
    std::vector<real_t> tmc023((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_c=0;q_c<NV;++q_c){
              for (int q_l=0;q_l<NO;++q_l){
                acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_l,q_c);
              }
            }
          }
          tmc023[(q_b)*NMo+r_m] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                acc += tmc023[(q_b)*NMo+r_m]*siP(r_j,q_m,q_i,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc024: -0.500000 * eri_ovov[j,c,k,d] * ry[e,i,a,d] * ry[b,k,e,c]
    std::vector<real_t> tmc024((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_e,q_c);
                }
              }
              tmc024[(((r_b)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc024[(((r_b)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_a,q_d);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc025: +0.375000 * eri_ovov[j,c,k,d] * ry[e,i,a,c] * ry[b,k,e,d]
    std::vector<real_t> tmc025((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_e,q_d);
                }
              }
              tmc025[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc025[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_a,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc026: -0.125000 * eri_ovov[j,c,k,d] * ry[e,i,a,c] * ry[b,k,d,e]
    std::vector<real_t> tmc026((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_d,q_e);
                }
              }
              tmc026[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc026[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_a,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc027: -0.125000 * eri_ovov[j,c,k,d] * ry[e,i,d,c] * ry[b,k,e,a]
    std::vector<real_t> tmc027((size_t)NMv*NO*NO*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_d,q_c);
                }
              }
              tmc027[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc027[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k]*seA(r_b,q_k,q_e,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc028: -0.125000 * eri_ovov[j,c,k,d] * ry[e,i,c,d] * ry[b,k,e,a]
    std::vector<real_t> tmc028((size_t)NMv*NO*NO*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_c,q_d);
                }
              }
              tmc028[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc028[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k]*seA(r_b,q_k,q_e,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc029: -0.125000 * eri_ovov[j,c,k,d] * ry[e,k,d,a] * ry[b,i,e,c]
    std::vector<real_t> tmc029((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_a);
                }
              }
              tmc029[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc029[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_e,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc030: +0.375000 * eri_ovov[j,c,k,d] * ry[e,k,a,d] * ry[b,i,e,c]
    std::vector<real_t> tmc030((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_d);
                }
              }
              tmc030[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc030[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_e,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc031: -0.250000 * eri_ovov[j,c,k,d] * ry[e,k,c,a] * ry[b,i,e,d]
    std::vector<real_t> tmc031((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_a);
                }
              }
              tmc031[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc031[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_e,q_d);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc032: +0.500000 * eri_ovov[j,c,k,d] * ry[e,i,d,a] * ry[b,k,c,e]
    std::vector<real_t> tmc032((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_c,q_e);
                }
              }
              tmc032[(((r_b)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc032[(((r_b)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_d,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc033: -0.125000 * eri_ovov[j,c,k,d] * ry[e,i,c,a] * ry[b,k,d,e]
    std::vector<real_t> tmc033((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_d,q_e);
                }
              }
              tmc033[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc033[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_c,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc034: +0.375000 * eri_ovov[j,c,k,d] * ry[e,i,c,a] * ry[b,k,e,d]
    std::vector<real_t> tmc034((size_t)NMv*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_k,q_e,q_d);
                }
              }
              tmc034[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc034[(((r_b)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_e,q_i,q_c,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc035: +0.375000 * eri_ovov[j,c,k,d] * ry[e,k,a,d] * ry[b,i,c,e]
    std::vector<real_t> tmc035((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_d);
                }
              }
              tmc035[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc035[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_c,q_e);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc036: -0.125000 * eri_ovov[j,c,k,d] * ry[e,k,d,a] * ry[b,i,c,e]
    std::vector<real_t> tmc036((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_a);
                }
              }
              tmc036[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc036[(((q_a)*NV+q_c)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_c,q_e);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc037: -0.250000 * eri_ovov[j,c,k,d] * ry[e,k,a,c] * ry[b,i,d,e]
    std::vector<real_t> tmc037((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_c);
                }
              }
              tmc037[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc037[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_d,q_e);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc038: -0.125000 * eri_ovov[j,c,k,d] * ry[e,k,d,c] * ry[b,i,a,e]
    std::vector<real_t> tmc038((size_t)NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_j=0;q_j<NO;++q_j){
          real_t acc=0.0;
          for (int q_c=0;q_c<NV;++q_c){
            for (int q_k=0;q_k<NO;++q_k){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_c);
              }
            }
          }
          tmc038[(r_e)*NO+q_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                acc += tmc038[(r_e)*NO+q_j]*seA(r_b,q_i,q_a,q_e);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc039: +0.375000 * eri_ovov[j,c,k,d] * ry[e,k,c,d] * ry[b,i,a,e]
    std::vector<real_t> tmc039((size_t)NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_j=0;q_j<NO;++q_j){
          real_t acc=0.0;
          for (int q_c=0;q_c<NV;++q_c){
            for (int q_k=0;q_k<NO;++q_k){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_d);
              }
            }
          }
          tmc039[(r_e)*NO+q_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                acc += tmc039[(r_e)*NO+q_j]*seA(r_b,q_i,q_a,q_e);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc040: -0.250000 * eri_ovov[j,c,k,d] * ry[e,k,a,c] * ry[b,i,e,d]
    std::vector<real_t> tmc040((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_c);
                }
              }
              tmc040[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc040[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_e,q_d);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc041: -0.125000 * eri_ovov[j,c,k,d] * ry[e,k,d,c] * ry[b,i,e,a]
    std::vector<real_t> tmc041((size_t)NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_j=0;q_j<NO;++q_j){
          real_t acc=0.0;
          for (int q_c=0;q_c<NV;++q_c){
            for (int q_k=0;q_k<NO;++q_k){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_c);
              }
            }
          }
          tmc041[(r_e)*NO+q_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                acc += tmc041[(r_e)*NO+q_j]*seA(r_b,q_i,q_e,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc042: +0.375000 * eri_ovov[j,c,k,d] * ry[e,k,c,d] * ry[b,i,e,a]
    std::vector<real_t> tmc042((size_t)NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_j=0;q_j<NO;++q_j){
          real_t acc=0.0;
          for (int q_c=0;q_c<NV;++q_c){
            for (int q_k=0;q_k<NO;++q_k){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_d);
              }
            }
          }
          tmc042[(r_e)*NO+q_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                acc += tmc042[(r_e)*NO+q_j]*seA(r_b,q_i,q_e,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc043: -0.125000 * eri_ovov[j,c,k,d] * ry[e,i,d,c] * ry[b,k,a,e]
    std::vector<real_t> tmc043((size_t)NMv*NO*NO*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_d,q_c);
                }
              }
              tmc043[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc043[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k]*seA(r_b,q_k,q_a,q_e);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc044: -0.125000 * eri_ovov[j,c,k,d] * ry[e,i,c,d] * ry[b,k,a,e]
    std::vector<real_t> tmc044((size_t)NMv*NO*NO*NO, 0.0);
    #pragma omp parallel for
      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_c,q_d);
                }
              }
              tmc044[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc044[(((r_e)*NO+q_i)*NO+q_j)*NO+q_k]*seA(r_b,q_k,q_a,q_e);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc045: -0.250000 * eri_ovov[j,c,k,d] * ry[e,k,c,a] * ry[b,i,d,e]
    std::vector<real_t> tmc045((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_d=0;q_d<NV;++q_d){
          for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
            for (int q_j=0;q_j<NO;++q_j){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_a);
                }
              }
              tmc045[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                  acc += tmc045[(((q_a)*NV+q_d)*NMv+r_e)*NO+q_j]*seA(r_b,q_i,q_d,q_e);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc046: -0.250000 * eri_ovov[k,b,l,c] * rx[j,k,i,d] * ry[d,l,c,a]
    std::vector<real_t> tmc046((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_b,q_l,q_c)*seA(r_d,q_l,q_c,q_a);
                }
              }
              tmc046[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tmc046[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k]*siP(r_j,q_k,q_i,q_d);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc047: +0.750000 * eri_ovov[k,b,l,c] * rx[j,k,i,d] * ry[d,l,a,c]
    std::vector<real_t> tmc047((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_l=0;q_l<NO;++q_l){
                for (int q_c=0;q_c<NV;++q_c){
                  acc += eriov(q_k,q_b,q_l,q_c)*seA(r_d,q_l,q_a,q_c);
                }
              }
              tmc047[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tmc047[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k]*siP(r_j,q_k,q_i,q_d);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc048: -0.250000 * eri_ovov[k,c,l,b] * rx[j,k,i,d] * ry[d,l,c,a]
    std::vector<real_t> tmc048((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*seA(r_d,q_l,q_c,q_a);
                }
              }
              tmc048[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tmc048[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k]*siP(r_j,q_k,q_i,q_d);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc049: -0.250000 * eri_ovov[k,c,l,b] * rx[j,k,i,d] * ry[d,l,a,c]
    std::vector<real_t> tmc049((size_t)NV*NV*NMv*NO, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_b=0;q_b<NV;++q_b){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int q_k=0;q_k<NO;++q_k){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*seA(r_d,q_l,q_a,q_c);
                }
              }
              tmc049[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tmc049[(((q_a)*NV+q_b)*NMv+r_d)*NO+q_k]*siP(r_j,q_k,q_i,q_d);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc050: +0.375000 * eri_ovov[k,c,l,b] * rx[j,l,k,d] * ry[d,i,c,a]
    std::vector<real_t> tmc050((size_t)NV*NV*NMv*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_l,q_k,q_d);
                }
              }
              tmc050[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tmc050[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j]*seA(r_d,q_i,q_c,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc051: -0.125000 * eri_ovov[k,c,l,b] * rx[j,l,k,d] * ry[d,i,a,c]
    std::vector<real_t> tmc051((size_t)NV*NV*NMv*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_l,q_k,q_d);
                }
              }
              tmc051[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tmc051[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j]*seA(r_d,q_i,q_a,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc052: +0.375000 * eri_ovov[k,b,l,c] * rx[j,k,l,d] * ry[d,i,c,a]
    std::vector<real_t> tmc052((size_t)NV*NV*NMv*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_k,q_l,q_d);
                }
              }
              tmc052[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tmc052[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j]*seA(r_d,q_i,q_c,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc053: -0.125000 * eri_ovov[k,b,l,c] * rx[j,k,l,d] * ry[d,i,a,c]
    std::vector<real_t> tmc053((size_t)NV*NV*NMv*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_k,q_l,q_d);
                }
              }
              tmc053[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tmc053[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j]*seA(r_d,q_i,q_a,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc054: -0.125000 * eri_ovov[k,b,l,c] * rx[j,l,k,d] * ry[d,i,c,a]
    std::vector<real_t> tmc054((size_t)NV*NV*NMv*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_l,q_k,q_d);
                }
              }
              tmc054[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tmc054[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j]*seA(r_d,q_i,q_c,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc055: -0.125000 * eri_ovov[k,c,l,b] * rx[j,k,l,d] * ry[d,i,c,a]
    std::vector<real_t> tmc055((size_t)NV*NV*NMv*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_k,q_l,q_d);
                }
              }
              tmc055[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tmc055[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j]*seA(r_d,q_i,q_c,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc056: -0.500000 * eri_ovov[k,c,l,d] * rx[j,k,i,d] * ry[b,l,a,c]
    std::vector<real_t> tmc056((size_t)NV*NO*NMo*NO, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_l=0;q_l<NO;++q_l){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_d);
                }
              }
              tmc056[(((q_c)*NO+q_i)*NMo+r_j)*NO+q_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += tmc056[(((q_c)*NO+q_i)*NMo+r_j)*NO+q_l]*seA(r_b,q_l,q_a,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc057: -0.500000 * eri_ovov[k,c,l,d] * rx[j,k,i,a] * ry[b,l,d,c]
    std::vector<real_t> tmc057((size_t)NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_k=0;q_k<NO;++q_k){
          real_t acc=0.0;
          for (int q_c=0;q_c<NV;++q_c){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_k,q_c,q_l,q_d)*seA(r_b,q_l,q_d,q_c);
              }
            }
          }
          tmc057[(r_b)*NO+q_k] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                acc += tmc057[(r_b)*NO+q_k]*siP(r_j,q_k,q_i,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc058: +1.500000 * eri_ovov[k,c,l,d] * rx[j,k,i,a] * ry[b,l,c,d]
    std::vector<real_t> tmc058((size_t)NMv*NO, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_k=0;q_k<NO;++q_k){
          real_t acc=0.0;
          for (int q_c=0;q_c<NV;++q_c){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_k,q_c,q_l,q_d)*seA(r_b,q_l,q_c,q_d);
              }
            }
          }
          tmc058[(r_b)*NO+q_k] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                acc += tmc058[(r_b)*NO+q_k]*siP(r_j,q_k,q_i,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
            }
          }
        }
      }
    }
    { // term mc059: -0.500000 * eri_ovov[k,c,l,d] * rx[j,k,i,d] * ry[b,l,c,a]
    std::vector<real_t> tmc059((size_t)NV*NO*NMo*NO, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_l=0;q_l<NO;++q_l){
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_d);
                }
              }
              tmc059[(((q_c)*NO+q_i)*NMo+r_j)*NO+q_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += tmc059[(((q_c)*NO+q_i)*NMo+r_j)*NO+q_l]*seA(r_b,q_l,q_c,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc060: +0.500000 * eri_ovov[k,c,l,d] * rx[j,l,k,c] * ry[b,i,a,d]
    std::vector<real_t> tmc060((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_c=0;q_c<NV;++q_c){
              for (int q_l=0;q_l<NO;++q_l){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_c);
              }
            }
          }
          tmc060[(q_d)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                acc += tmc060[(q_d)*NMo+r_j]*seA(r_b,q_i,q_a,q_d);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc061: +0.500000 * eri_ovov[k,c,l,d] * rx[j,k,l,d] * ry[b,i,a,c]
    std::vector<real_t> tmc061((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_d);
              }
            }
          }
          tmc061[(q_c)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                acc += tmc061[(q_c)*NMo+r_j]*seA(r_b,q_i,q_a,q_c);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc062: -0.250000 * eri_ovov[k,c,l,d] * rx[j,k,l,c] * ry[b,i,a,d]
    std::vector<real_t> tmc062((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_c=0;q_c<NV;++q_c){
              for (int q_l=0;q_l<NO;++q_l){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_c);
              }
            }
          }
          tmc062[(q_d)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                acc += tmc062[(q_d)*NMo+r_j]*seA(r_b,q_i,q_a,q_d);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc063: -0.250000 * eri_ovov[k,c,l,d] * rx[j,l,k,d] * ry[b,i,a,c]
    std::vector<real_t> tmc063((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_d);
              }
            }
          }
          tmc063[(q_c)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                acc += tmc063[(q_c)*NMo+r_j]*seA(r_b,q_i,q_a,q_c);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc064: -0.125000 * eri_ovov[k,b,l,c] * rx[j,l,k,d] * ry[d,i,a,c]
    std::vector<real_t> tmc064((size_t)NV*NV*NMv*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_l,q_k,q_d);
                }
              }
              tmc064[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tmc064[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j]*seA(r_d,q_i,q_a,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc065: -0.125000 * eri_ovov[k,c,l,b] * rx[j,k,l,d] * ry[d,i,a,c]
    std::vector<real_t> tmc065((size_t)NV*NV*NMv*NMo, 0.0);
    #pragma omp parallel for
      for (int q_b=0;q_b<NV;++q_b){
        for (int q_c=0;q_c<NV;++q_c){
          for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_k,q_l,q_d);
                }
              }
              tmc065[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int q_b=0;q_b<NV;++q_b){
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                  acc += tmc065[(((q_b)*NV+q_c)*NMv+r_d)*NMo+r_j]*seA(r_d,q_i,q_a,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc066: -0.250000 * eri_ovov[k,c,l,d] * rx[j,l,k,a] * ry[b,i,c,d]
    std::vector<real_t> tmc066((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int q_d=0;q_d<NV;++q_d){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_a);
                }
              }
              tmc066[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += tmc066[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j]*seA(r_b,q_i,q_c,q_d);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc067: -0.250000 * eri_ovov[k,c,l,d] * rx[j,l,k,a] * ry[b,i,d,c]
    std::vector<real_t> tmc067((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int q_d=0;q_d<NV;++q_d){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_a);
                }
              }
              tmc067[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += tmc067[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j]*seA(r_b,q_i,q_d,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc068: -0.250000 * eri_ovov[k,c,l,d] * rx[j,k,l,a] * ry[b,i,d,c]
    std::vector<real_t> tmc068((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int q_d=0;q_d<NV;++q_d){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_a);
                }
              }
              tmc068[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += tmc068[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j]*seA(r_b,q_i,q_d,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc069: -0.250000 * eri_ovov[k,c,l,d] * rx[j,k,l,a] * ry[b,i,c,d]
    std::vector<real_t> tmc069((size_t)NV*NV*NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_a=0;q_a<NV;++q_a){
        for (int q_c=0;q_c<NV;++q_c){
          for (int q_d=0;q_d<NV;++q_d){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_l=0;q_l<NO;++q_l){
                  acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_a);
                }
              }
              tmc069[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += tmc069[(((q_a)*NV+q_c)*NV+q_d)*NMo+r_j]*seA(r_b,q_i,q_c,q_d);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc070: +0.500000 * eri_ovov[k,c,l,d] * rx[j,l,k,c] * ry[b,i,d,a]
    std::vector<real_t> tmc070((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_c=0;q_c<NV;++q_c){
              for (int q_l=0;q_l<NO;++q_l){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_c);
              }
            }
          }
          tmc070[(q_d)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                acc += tmc070[(q_d)*NMo+r_j]*seA(r_b,q_i,q_d,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc071: +0.500000 * eri_ovov[k,c,l,d] * rx[j,k,l,d] * ry[b,i,c,a]
    std::vector<real_t> tmc071((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_d);
              }
            }
          }
          tmc071[(q_c)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                acc += tmc071[(q_c)*NMo+r_j]*seA(r_b,q_i,q_c,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc072: -0.250000 * eri_ovov[k,c,l,d] * rx[j,k,l,c] * ry[b,i,d,a]
    std::vector<real_t> tmc072((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_c=0;q_c<NV;++q_c){
              for (int q_l=0;q_l<NO;++q_l){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_c);
              }
            }
          }
          tmc072[(q_d)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                acc += tmc072[(q_d)*NMo+r_j]*seA(r_b,q_i,q_d,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc073: -0.250000 * eri_ovov[k,c,l,d] * rx[j,l,k,d] * ry[b,i,c,a]
    std::vector<real_t> tmc073((size_t)NV*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
          real_t acc=0.0;
          for (int q_k=0;q_k<NO;++q_k){
            for (int q_l=0;q_l<NO;++q_l){
              for (int q_d=0;q_d<NV;++q_d){
                acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_d);
              }
            }
          }
          tmc073[(q_c)*NMo+r_j] = acc;
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                acc += tmc073[(q_c)*NMo+r_j]*seA(r_b,q_i,q_c,q_a);
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc074: -0.250000 * eri_ovov[j,c,k,d] * rx[l,k,i,d] * ry[b,l,a,c]
    std::vector<real_t> tmc074((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_d);
                }
              }
              tmc074[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tmc074[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_a,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc075: +0.500000 * eri_ovov[j,c,k,d] * rx[l,i,k,d] * ry[b,l,a,c]
    std::vector<real_t> tmc075((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_d);
                }
              }
              tmc075[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tmc075[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_a,q_c);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc076: -0.250000 * eri_ovov[j,c,k,d] * rx[l,i,k,c] * ry[b,l,a,d]
    std::vector<real_t> tmc076((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_c);
                }
              }
              tmc076[(((q_d)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tmc076[(((q_d)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_a,q_d);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc077: -0.250000 * eri_ovov[j,c,k,d] * rx[l,i,k,a] * ry[b,l,d,c]
    std::vector<real_t> tmc077((size_t)NMv*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_j=0;q_j<NO;++q_j){
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_l,q_d,q_c);
                }
              }
              tmc077[(((r_b)*NO+q_j)*NO+q_k)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tmc077[(((r_b)*NO+q_j)*NO+q_k)*NMo+r_l]*siP(r_l,q_i,q_k,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc078: -0.250000 * eri_ovov[j,c,k,d] * rx[l,k,i,a] * ry[b,l,d,c]
    std::vector<real_t> tmc078((size_t)NMv*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_j=0;q_j<NO;++q_j){
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_l,q_d,q_c);
                }
              }
              tmc078[(((r_b)*NO+q_j)*NO+q_k)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tmc078[(((r_b)*NO+q_j)*NO+q_k)*NMo+r_l]*siP(r_l,q_k,q_i,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc079: -0.250000 * eri_ovov[j,c,k,d] * rx[l,i,k,a] * ry[b,l,c,d]
    std::vector<real_t> tmc079((size_t)NMv*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_j=0;q_j<NO;++q_j){
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_l,q_c,q_d);
                }
              }
              tmc079[(((r_b)*NO+q_j)*NO+q_k)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tmc079[(((r_b)*NO+q_j)*NO+q_k)*NMo+r_l]*siP(r_l,q_i,q_k,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc080: +0.750000 * eri_ovov[j,c,k,d] * rx[l,k,i,a] * ry[b,l,c,d]
    std::vector<real_t> tmc080((size_t)NMv*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
        for (int q_j=0;q_j<NO;++q_j){
          for (int q_k=0;q_k<NO;++q_k){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*seA(r_b,q_l,q_c,q_d);
                }
              }
              tmc080[(((r_b)*NO+q_j)*NO+q_k)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tmc080[(((r_b)*NO+q_j)*NO+q_k)*NMo+r_l]*siP(r_l,q_k,q_i,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +7.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc081: +0.500000 * eri_ovov[j,c,k,d] * rx[l,i,k,d] * ry[b,l,c,a]
    std::vector<real_t> tmc081((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_d);
                }
              }
              tmc081[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tmc081[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_c,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc082: -0.250000 * eri_ovov[j,c,k,d] * rx[l,k,i,d] * ry[b,l,c,a]
    std::vector<real_t> tmc082((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_c=0;q_c<NV;++q_c){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_k=0;q_k<NO;++q_k){
                for (int q_d=0;q_d<NV;++q_d){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_d);
                }
              }
              tmc082[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tmc082[(((q_c)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_c,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    { // term mc083: -0.250000 * eri_ovov[j,c,k,d] * rx[l,i,k,c] * ry[b,l,d,a]
    std::vector<real_t> tmc083((size_t)NV*NO*NO*NMo, 0.0);
    #pragma omp parallel for
      for (int q_d=0;q_d<NV;++q_d){
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
              real_t acc=0.0;
              for (int q_c=0;q_c<NV;++q_c){
                for (int q_k=0;q_k<NO;++q_k){
                  acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_c);
                }
              }
              tmc083[(((q_d)*NO+q_i)*NO+q_j)*NMo+r_l] = acc;
            }
          }
        }
      }
    #pragma omp parallel for
      for (int q_i=0;q_i<NO;++q_i){
        for (int q_a=0;q_a<NV;++q_a){
          for (int q_j=0;q_j<NO;++q_j){
            for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
              real_t acc=0.0;
              for (int q_d=0;q_d<NV;++q_d){
                for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                  acc += tmc083[(((q_d)*NO+q_i)*NO+q_j)*NMo+r_l]*seA(r_b,q_l,q_d,q_a);
                }
              }
              proj_route_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
            }
          }
        }
      }
    }
    if (std::getenv("GANSU_STEOM_PROJ_VALIDATE")) {
      std::vector<real_t> ref_ph(proj_route_ph.size(),0.0), ref_mc(proj_route_mc.size(),0.0);
      { // naive reference for proj_route_ph
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  acc += fov(q_j,q_c)*seA(r_b,q_i,q_c,q_a);
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  acc += fov(q_j,q_c)*seA(r_b,q_i,q_a,q_c);
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.5000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_d=0;q_d<NV;++q_d){
                    acc += wvovv(q_a,q_j,q_c,q_d)*seA(r_b,q_i,q_c,q_d);
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.5000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_d=0;q_d<NV;++q_d){
                    acc += wvovv(q_a,q_j,q_c,q_d)*seA(r_b,q_i,q_d,q_c);
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    acc += wooov(q_k,q_j,q_i,q_c)*seA(r_b,q_k,q_c,q_a);
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    acc += wooov(q_k,q_j,q_i,q_c)*seA(r_b,q_k,q_a,q_c);
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_a,q_d)*seA(r_b,q_k,q_e,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.1250000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_a,q_c)*seA(r_b,q_k,q_e,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.1250000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_a,q_c)*seA(r_b,q_k,q_d,q_e);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_d,q_c)*seA(r_b,q_k,q_e,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_c,q_d)*seA(r_b,q_k,q_e,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_a)*seA(r_b,q_i,q_e,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_d)*seA(r_b,q_i,q_e,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.1250000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_a)*seA(r_b,q_i,q_e,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_d,q_a)*seA(r_b,q_k,q_c,q_e);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_c,q_a)*seA(r_b,q_k,q_d,q_e);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_c,q_a)*seA(r_b,q_k,q_e,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_d)*seA(r_b,q_i,q_c,q_e);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_a)*seA(r_b,q_i,q_c,q_e);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_c)*seA(r_b,q_i,q_d,q_e);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_c)*seA(r_b,q_i,q_a,q_e);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_d)*seA(r_b,q_i,q_a,q_e);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.1250000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_c)*seA(r_b,q_i,q_e,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_c)*seA(r_b,q_i,q_e,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_d)*seA(r_b,q_i,q_e,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_d,q_c)*seA(r_b,q_k,q_a,q_e);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_c,q_d)*seA(r_b,q_k,q_a,q_e);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_a)*seA(r_b,q_i,q_d,q_e);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_k,q_i,q_d)*seA(r_d,q_l,q_c,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_k,q_i,q_d)*seA(r_d,q_l,q_a,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_l,q_k,q_d)*seA(r_d,q_i,q_c,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_k,q_l,q_d)*seA(r_d,q_i,q_c,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_d)*seA(r_b,q_l,q_a,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.5000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_d)*seA(r_b,q_l,q_c,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_c)*seA(r_b,q_i,q_a,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_d)*seA(r_b,q_i,q_a,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_c)*seA(r_b,q_i,q_a,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_d)*seA(r_b,q_i,q_a,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_l,q_k,q_d)*seA(r_d,q_i,q_a,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_k,q_l,q_d)*seA(r_d,q_i,q_a,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_a)*seA(r_b,q_i,q_c,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_a)*seA(r_b,q_i,q_d,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_a)*seA(r_b,q_i,q_d,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_a)*seA(r_b,q_i,q_c,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_c)*seA(r_b,q_i,q_d,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_d)*seA(r_b,q_i,q_c,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_c)*seA(r_b,q_i,q_d,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_d)*seA(r_b,q_i,q_c,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_d)*seA(r_b,q_l,q_a,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_d)*seA(r_b,q_l,q_a,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_c)*seA(r_b,q_l,q_a,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_a)*seA(r_b,q_l,q_d,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_a)*seA(r_b,q_l,q_c,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_d)*seA(r_b,q_l,q_c,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_d)*seA(r_b,q_l,q_c,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_c)*seA(r_b,q_l,q_d,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  acc += fov(q_k,q_b)*siP(r_j,q_i,q_k,q_a);
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    acc += wooov(q_k,q_l,q_i,q_b)*siP(r_j,q_k,q_l,q_a);
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    acc += wvovv(q_a,q_k,q_b,q_c)*siP(r_j,q_i,q_k,q_c);
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +2.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    acc += wvovv(q_a,q_k,q_b,q_c)*siP(r_j,q_k,q_i,q_c);
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    acc += wvovv(q_a,q_k,q_c,q_b)*siP(r_j,q_i,q_k,q_c);
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    acc += wooov(q_j,q_k,q_i,q_c)*seA(r_b,q_k,q_c,q_a);
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    acc += wooov(q_j,q_k,q_i,q_c)*seA(r_b,q_k,q_a,q_c);
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.5000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_i,q_k,q_a)*siP(r_j,q_l,q_m,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_i,q_k,q_a)*siP(r_j,q_m,q_l,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_i,q_k,q_a)*siP(r_j,q_m,q_l,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_i,q_k,q_c)*siP(r_j,q_m,q_l,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_i,q_c)*siP(r_j,q_l,q_m,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_i,q_k,q_c)*siP(r_j,q_m,q_l,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_i,q_c)*siP(r_j,q_m,q_l,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_l,q_k,q_a)*siP(r_j,q_i,q_m,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_l,q_k,q_a)*siP(r_j,q_m,q_i,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_l,q_a)*siP(r_j,q_i,q_m,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_l,q_k,q_a)*siP(r_j,q_i,q_m,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_l,q_a)*siP(r_j,q_i,q_m,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_l,q_a)*siP(r_j,q_m,q_i,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_l,q_k,q_c)*siP(r_j,q_i,q_m,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_l,q_c)*siP(r_j,q_i,q_m,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_l,q_k,q_c)*siP(r_j,q_i,q_m,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_l,q_c)*siP(r_j,q_i,q_m,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_a,q_d)*seA(r_b,q_k,q_c,q_e);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_d,q_a)*seA(r_b,q_k,q_e,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_i,q_k,q_d)*seA(r_d,q_l,q_c,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_i,q_k,q_d)*seA(r_d,q_l,q_a,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_i,q_k,q_d)*seA(r_d,q_l,q_c,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_i,q_k,q_d)*seA(r_d,q_l,q_a,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_c)*seA(r_b,q_l,q_a,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_c)*seA(r_b,q_l,q_d,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_i,q_k,q_c)*seA(r_b,q_l,q_a,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -3.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_i,q_k,q_c)*seA(r_b,q_l,q_d,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_i,q_k,q_d)*seA(r_b,q_l,q_a,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_i,q_k,q_d)*seA(r_b,q_l,q_c,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_i,q_k,q_a)*seA(r_b,q_l,q_c,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_i,q_k,q_a)*seA(r_b,q_l,q_d,q_c);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_c)*seA(r_b,q_l,q_a,q_d);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_c)*seA(r_b,q_l,q_d,q_a);
                      }
                    }
                  }
                }
                ref_ph[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
      }
      { // naive reference for proj_route_mc
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  acc += fov(q_k,q_b)*siP(r_j,q_k,q_i,q_a);
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    acc += wooov(q_k,q_l,q_i,q_b)*siP(r_j,q_l,q_k,q_a);
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    acc += wvovv(q_a,q_k,q_c,q_b)*siP(r_j,q_k,q_i,q_c);
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  acc += fov(q_j,q_c)*seA(r_b,q_i,q_c,q_a);
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  acc += fov(q_j,q_c)*seA(r_b,q_i,q_a,q_c);
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_d=0;q_d<NV;++q_d){
                    acc += wvovv(q_a,q_j,q_c,q_d)*seA(r_b,q_i,q_c,q_d);
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_d=0;q_d<NV;++q_d){
                    acc += wvovv(q_a,q_j,q_c,q_d)*seA(r_b,q_i,q_d,q_c);
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    acc += wooov(q_k,q_j,q_i,q_c)*seA(r_b,q_k,q_c,q_a);
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    acc += wooov(q_k,q_j,q_i,q_c)*seA(r_b,q_k,q_a,q_c);
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_i,q_a)*siP(r_j,q_l,q_m,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_i,q_k,q_a)*siP(r_j,q_l,q_m,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_i,q_a)*siP(r_j,q_m,q_l,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_i,q_a)*siP(r_j,q_l,q_m,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_i,q_a)*siP(r_j,q_m,q_l,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_i,q_k,q_c)*siP(r_j,q_l,q_m,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.0000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_i,q_c)*siP(r_j,q_l,q_m,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_i,q_c)*siP(r_j,q_m,q_l,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_i,q_k,q_c)*siP(r_j,q_l,q_m,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_l,q_k,q_a)*siP(r_j,q_m,q_i,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_l,q_a)*siP(r_j,q_m,q_i,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_l,q_k,q_c)*siP(r_j,q_m,q_i,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_k,q_l,q_c)*siP(r_j,q_m,q_i,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_m,q_l,q_k,q_c)*siP(r_j,q_m,q_i,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_m=0;r_m<NMo;++r_m){ const int q_m=active_occ_idx_[r_m];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_m,q_k,q_l,q_c)*siP(r_j,q_m,q_i,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_a,q_d)*seA(r_b,q_k,q_e,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_a,q_c)*seA(r_b,q_k,q_e,q_d);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_a,q_c)*seA(r_b,q_k,q_d,q_e);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_d,q_c)*seA(r_b,q_k,q_e,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_c,q_d)*seA(r_b,q_k,q_e,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_a)*seA(r_b,q_i,q_e,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_d)*seA(r_b,q_i,q_e,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_a)*seA(r_b,q_i,q_e,q_d);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_d,q_a)*seA(r_b,q_k,q_c,q_e);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_c,q_a)*seA(r_b,q_k,q_d,q_e);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_c,q_a)*seA(r_b,q_k,q_e,q_d);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_d)*seA(r_b,q_i,q_c,q_e);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_a)*seA(r_b,q_i,q_c,q_e);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_c)*seA(r_b,q_i,q_d,q_e);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_c)*seA(r_b,q_i,q_a,q_e);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_d)*seA(r_b,q_i,q_a,q_e);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_a,q_c)*seA(r_b,q_i,q_e,q_d);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_d,q_c)*seA(r_b,q_i,q_e,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_d)*seA(r_b,q_i,q_e,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_d,q_c)*seA(r_b,q_k,q_a,q_e);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_i,q_c,q_d)*seA(r_b,q_k,q_a,q_e);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_e=0;r_e<NMv;++r_e){ const int q_e=active_vir_idx_[r_e];
                        acc += eriov(q_j,q_c,q_k,q_d)*seA(r_e,q_k,q_c,q_a)*seA(r_b,q_i,q_d,q_e);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_k,q_i,q_d)*seA(r_d,q_l,q_c,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_k,q_i,q_d)*seA(r_d,q_l,q_a,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_k,q_i,q_d)*seA(r_d,q_l,q_c,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_k,q_i,q_d)*seA(r_d,q_l,q_a,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_l,q_k,q_d)*seA(r_d,q_i,q_c,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_l,q_k,q_d)*seA(r_d,q_i,q_a,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_k,q_l,q_d)*seA(r_d,q_i,q_c,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +3.7500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_k,q_l,q_d)*seA(r_d,q_i,q_a,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_l,q_k,q_d)*seA(r_d,q_i,q_c,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_k,q_l,q_d)*seA(r_d,q_i,q_c,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_d)*seA(r_b,q_l,q_a,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_a)*seA(r_b,q_l,q_d,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_a)*seA(r_b,q_l,q_c,q_d);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +1.5000000000000000e+00*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_i,q_d)*seA(r_b,q_l,q_c,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_c)*seA(r_b,q_i,q_a,q_d);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_d)*seA(r_b,q_i,q_a,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_c)*seA(r_b,q_i,q_a,q_d);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_d)*seA(r_b,q_i,q_a,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_l=0;q_l<NO;++q_l){
                    for (int q_c=0;q_c<NV;++q_c){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_b,q_l,q_c)*siP(r_j,q_l,q_k,q_d)*seA(r_d,q_i,q_a,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int q_b=0;q_b<NV;++q_b){
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int r_d=0;r_d<NMv;++r_d){ const int q_d=active_vir_idx_[r_d];
                        acc += eriov(q_k,q_c,q_l,q_b)*siP(r_j,q_k,q_l,q_d)*seA(r_d,q_i,q_a,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -1.2500000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_a)*seA(r_b,q_i,q_c,q_d);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_a)*seA(r_b,q_i,q_d,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_a)*seA(r_b,q_i,q_d,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_a)*seA(r_b,q_i,q_c,q_d);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_c)*seA(r_b,q_i,q_d,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_d)*seA(r_b,q_i,q_c,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_k,q_l,q_c)*seA(r_b,q_i,q_d,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int r_j=0;r_j<NMo;++r_j){ const int q_j=active_occ_idx_[r_j];
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_k=0;q_k<NO;++q_k){
                  for (int q_c=0;q_c<NV;++q_c){
                    for (int q_l=0;q_l<NO;++q_l){
                      for (int q_d=0;q_d<NV;++q_d){
                        acc += eriov(q_k,q_c,q_l,q_d)*siP(r_j,q_l,q_k,q_d)*seA(r_b,q_i,q_c,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_d)*seA(r_b,q_l,q_a,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_d)*seA(r_b,q_l,q_a,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_c)*seA(r_b,q_l,q_a,q_d);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_a)*seA(r_b,q_l,q_d,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_a)*seA(r_b,q_l,q_d,q_c);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_a)*seA(r_b,q_l,q_c,q_d);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_a)*seA(r_b,q_l,q_c,q_d);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +7.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_d)*seA(r_b,q_l,q_c,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += +5.0000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_k,q_i,q_d)*seA(r_b,q_l,q_c,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
        for (int q_i=0;q_i<NO;++q_i){
          for (int q_a=0;q_a<NV;++q_a){
            for (int q_j=0;q_j<NO;++q_j){
              for (int r_b=0;r_b<NMv;++r_b){ const int q_b=active_vir_idx_[r_b];
                real_t acc=0.0;
                for (int q_c=0;q_c<NV;++q_c){
                  for (int q_k=0;q_k<NO;++q_k){
                    for (int q_d=0;q_d<NV;++q_d){
                      for (int r_l=0;r_l<NMo;++r_l){ const int q_l=active_occ_idx_[r_l];
                        acc += eriov(q_j,q_c,q_k,q_d)*siP(r_l,q_i,q_k,q_c)*seA(r_b,q_l,q_d,q_a);
                      }
                    }
                  }
                }
                ref_mc[(((size_t)q_i*NV+q_a)*NO+q_j)*NV+q_b] += -2.5000000000000000e-01*acc;
              }
            }
          }
        }
      }
      real_t dph=0.0,dmc=0.0;
      for (size_t x=0;x<proj_route_ph.size();++x) dph=std::max(dph,std::fabs(proj_route_ph[x]-ref_ph[x]));
      for (size_t x=0;x<proj_route_mc.size();++x) dmc=std::max(dmc,std::fabs(proj_route_mc[x]-ref_mc[x]));
      std::cout << "[proj self-check] factorized vs naive: route_ph max|d| = " << std::scientific
                << dph << ", route_mc max|d| = " << dmc << " (expect <=1e-12)" << std::defaultfloat << std::endl;
    }
